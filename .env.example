# Django settings loaded from environment variables.
# Copy this file to .env and update the values.
# .env is git-ignored; .env.example is committed to version control.

DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True